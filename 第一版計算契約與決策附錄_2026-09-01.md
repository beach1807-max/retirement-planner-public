# 第一版計算契約與決策附錄

更新時間：2026-09-01 22:05（臺灣時間）  
契約版本：`calculation-contract-v0.1`

適用文件：〈個人與家庭退休資產規劃 PWA 方案統整〉

---

## 一、用途與效力

本附錄用來固定第一版 Projection Engine（未來資產預測引擎）與 Retirement Engine（退休計算引擎）的輸入、運算順序、結果語意及驗收標準，避免不同畫面、資料來源或開發階段各自採用不同公式。

第一版的計算目標只有兩個：

1. 在指定退休月份下，預測主要規劃人到規劃終點前的家庭可用退休資產。
2. 從允許的候選月份中，找出主要規劃人最早可以退休的月份與年齡。

本附錄中的「已定案」規則，第一版不得由畫面或開發者自行改變；若未來修改，必須建立新的契約版本並保留舊資料可追溯性。

---

## 二、計算輸入契約

計算引擎只接收已整理、已驗證的純資料物件，不直接讀取 IndexedDB（瀏覽器本機資料庫）、畫面狀態、市場行情或系統時間。

### 2.1 必要輸入

```text
CalculationInput

contractVersion
calculationId
calculationBaseDate
household
members[]
assets[]
contributions[]
retirementPlan
assumptions
ruleVersion
```

最低要求：

- `calculationBaseDate`：計算基準日，由應用層明確傳入。
- `household.primaryMemberId`：主要規劃人。
- 主要規劃人的出生日期與規劃終點年齡。
- 至少一筆有效退休資產，或明確提供為 0 的起始退休資產。
- 退休生活費，金額以計算基準日的今天購買力輸入。
- 年化報酬率、年化通膨率及安全準備金設定。

### 2.2 金額、日期與利率共同規格

- 金額運算使用十進位數值，不使用二進位浮點數直接累積財務金額。
- 內部主計算幣別第一版為 TWD（新臺幣）。外幣資產先由應用層依已保存匯率換算，再交給計算引擎。
- 資料庫金額可保存到小數點後 2 位；引擎運算過程保留較高精度，只在輸出與畫面顯示時四捨五入。
- 日期採 ISO 8601 格式；計算期間統一正規化為月份。
- 年化報酬率與通膨率皆定義為「有效年利率」，不是直接除以 12 的名目年利率。
- 所有假設都必須包含來源版本或使用者設定值，不得散落為程式中的固定常數。

### 2.3 未提供不等於 0

```text
資料不存在／status = notProvided
→ 未提供，不納入運算，產生 Warning（提醒）

資料存在／status = provided／amount = 0
→ 明確提供為 0，納入運算，不產生缺漏提醒

status = notApplicable
→ 明確不適用，不納入運算，也不視為缺漏
```

解析失敗、空字串、`null` 或 `undefined` 不得自動轉成 0。

---

## 三、月份時間軸與運算順序

### 3.1 基準月份

第一個計算月份為 `calculationBaseDate` 所在月份。年齡由出生日期與月份計算，不保存固定的目前年齡。

候選退休月份從使用者設定的最早允許退休月份開始，逐月向後搜尋；前台可只顯示「X 歲 Y 個月」或四捨五入後年齡，但引擎輸出必須保留精確月份。

### 3.2 每月事件順序

每一月份固定依下列順序處理：

1. 取得月初資產餘額。
2. 加入當月月初生效的退休收入或其他外部流入。
3. 扣除退休生活費與當月一次性支出。
4. 若可用餘額已低於 0，該候選退休月份立即判定失敗。
5. 對扣除支出後的剩餘資產計算當月投資報酬。
6. 在月底加入仍有效的每月投入。
7. 產生月底餘額，作為下個月月初餘額。

第一版公式：

```text
月底餘額
=（月初餘額 + 月初外部流入 − 當月退休支出 − 一次性支出）
  ×（1 + 月報酬率）
  + 月底投入
```

每月投入採月底投入，因此投入當月不計算投資報酬，從下一個月起開始計息。這項規則需固定，避免不同模組出現半個月或整個月報酬差異。

### 3.3 年化利率換算

```text
月報酬率 = (1 + 年化報酬率)^(1/12) − 1

第 n 月名目退休支出
= 基準日實質月支出 × (1 + 年化通膨率)^(n/12)
```

例如年化報酬率 6%，月報酬率不是固定使用 0.5%，而是依有效年利率公式換算。

引擎內部以名目金額建立時間軸；畫面預設顯示今天購買力的實質金額：

```text
第 n 月實質金額
= 第 n 月名目金額 ÷ (1 + 年化通膨率)^(n/12)
```

---

## 四、家庭資源納入契約

### 4.1 家庭退休計算可使用的資產

以主要規劃人 A 的家庭退休問題為例，可納入：

- A 的 `personal` 資產，且該資產允許用於 A 的退休。
- `household` 家庭可用退休資產。
- 伴侶 B 明確標記為 `household` 的退休資產。
- `joint` 共同資產中明確允許家庭退休使用的完整可用價值。

不得納入：

- B 標記為 `personal` 的個人退休資產。
- `retirementUsageScope = excluded` 的資產。
- 尚未到 `availableFrom` 的資產。
- 僅納入總資產、但未納入退休用途的資產。

### 4.2 避免重複計算

- 家庭檢視中的共同資產只計一次，不按 A、B 持分加總後再重複加入。
- 個人檢視可依持分顯示 `joint` 資產，但個人顯示值不能改變家庭總額。
- `household` 資料只屬家庭層級，不複製到個人成員資料。

### 4.3 每月投入的退休行為

每筆 Contribution（可投入資金）至少補充：

```text
destinationAssetId 或 returnProfileId
startDate
endRule
endDate（固定日期模式才需要）
```

`endRule` 第一版支援：

```text
ownerRetirement     所屬成員退休時停止
primaryRetirement   主要規劃人退休時停止
fixedDate           指定日期停止
planEnd             持續到規劃終點
```

預設採 `ownerRetirement`。若伴侶的投入會延續到主要規劃人退休後，必須有伴侶預計退休月份或其他明確結束規則；系統不得自行猜測。

每月投入必須指定投向哪一項資產或哪一種 Return Profile（報酬設定檔）。未指定時屬 Error（錯誤），不得默認全部投入股票或現金。

### 4.4 支出與提領

第一版退休生活費視為家庭層級支出。符合退休用途的資產依當月可用餘額比例提領，以維持既有資產配置；第一版不計交易稅、手續費、所得稅及個別資產出售限制。

若未來加入稅務最佳化、現金優先或指定提領順序，必須新增 Withdrawal Strategy（提領策略）版本，不直接改變本契約的歷史結果。

---

## 五、退休達標判斷契約

### 5.1 候選退休月份

對每一個候選退休月份，系統建立獨立時間軸：

- 主要規劃人符合 `primaryRetirement` 或 `ownerRetirement` 的投入，自該退休月份起停止。
- 退休生活費自該退休月份起發生。
- 伴侶的投入、退休收入與其他現金流依各自日期及結束規則繼續。
- 不修改正式資料；候選退休月份只是本次試算覆寫值。

### 5.2 第一版「可以退休」定義

候選月份同時符合以下條件，才判定為可以退休：

1. 從退休月份到規劃終點的每個月份，家庭可用退休資產均未低於 0。
2. 所有已知且已納入的一次性支出都能支付。
3. 規劃終點的剩餘實質資產不低於安全準備金與指定遺產目標之和。
4. 計算期間沒有 Error（錯誤）。Warning（提醒）不阻止計算。

```text
期末最低要求
= 安全準備金 + 指定遺產目標
```

第一版預設安全準備金與遺產目標皆為 0，但欄位必須存在並由設定傳入，不得寫死在引擎內。

若到規劃終點時資產剛好為 0，且最低要求也是 0，視為達標。

### 5.3 最早可退休月份

系統逐月測試候選月份，第一個達標月份即為最早可退休月份。若所有候選月份都不達標，回傳 `notAchievableWithinHorizon`，不得捏造超出規劃終點的退休年齡。

此結果只代表：

> 依目前已提供資料與指定假設，在規劃期間內沒有出現資金缺口。

不代表保證未來報酬、法規或實際支出一定如此。

---

## 六、輸出、錯誤與可追溯性

### 6.1 最低輸出

```text
CalculationResult

calculationId
contractVersion
ruleVersion
calculationBaseDate
inputHash
status
earliestRetirementMonth
retirementAgeInMonths
retirementAssetsAtRetirement
endingAssetsNominal
endingAssetsReal
monthlyTimeline[]
warnings[]
errors[]
includedDataSummary
excludedDataSummary
```

`monthlyTimeline[]` 至少保留月份、月初餘額、流入、支出、報酬、投入及月底餘額，讓測試與結果解釋可以追溯。

### 6.2 Error（錯誤）

以下情況阻止計算：

- 主要規劃人、出生日期、規劃終點或退休生活費缺失。
- 出生日期在計算基準日之後。
- 規劃終點早於候選退休月份。
- 金額、持分、日期或利率格式無效。
- 共同持分合計超出允許範圍。
- 每月投入沒有指定投資目的地或報酬設定檔。
- 使用到引擎不支援的契約或規則版本。

### 6.3 Warning（提醒）

以下情況不阻止計算：

- 伴侶的收入、負債、勞保或勞退未提供。
- 部分資產明確排除退休計算。
- 市場價格或匯率不是最新資料。
- 使用固定報酬率而非隨機模擬。
- 尚未納入稅務、交易成本或法規模組。

畫面必須同時呈現重要 Warning（提醒）及「本次實際納入／排除的資料」，不可只顯示單一退休年齡。

---

## 七、第一版固定決策與暫緩決策

| 項目 | 第一版決策 | 狀態 |
|---|---|---|
| 計算粒度 | 每月 | 已定案 |
| 內部金額 | 名目金額；畫面預設實質金額 | 已定案 |
| 月利率換算 | 有效年利率公式 | 已定案 |
| 每月投入時點 | 月底投入、次月開始計息 | 已定案 |
| 家庭計算 | 重建單一時間軸，不相加個人結果 | 已定案 |
| 伴侶資料缺失 | 不推估、不當成 0、顯示提醒 | 已定案 |
| 第一版提領 | 依可用餘額比例提領 | 已定案 |
| 稅務與交易成本 | 暫不納入，介面保留 | 暫緩 |
| 勞保／勞退完整公式 | 後續規則模組 | 暫緩 |
| 隨機報酬 | 固定假設；Monte Carlo（蒙地卡羅模擬）後續加入 | 暫緩 |
| 資產出售優先順序 | 第一版不提供自訂 | 暫緩 |
| 多幣別時間序列 | 第一版先換算 TWD（新臺幣） | 暫緩 |

---

## 八、最低自動化驗收案例

1. **零報酬、零通膨案例**：起始退休資產 120 萬、退休生活費每月 1 萬、退休期間 120 個月、無其他現金流，期末為 0 且判定達標。
2. **有效年利率案例**：年化報酬率 6% 時，12 個月複利結果應接近 6%，不可用 0.5% 月利率造成偏差。
3. **未提供與 0 案例**：伴侶收入未提供時產生提醒；明確填寫 0 時不產生缺漏提醒；兩者資料狀態必須不同。
4. **伴侶資產範圍案例**：B 的 100 萬標記為 `household` 時納入 A 的家庭退休計算；改為 `personal` 或 `excluded` 後不得納入。
5. **共同資產案例**：A、B 各持有共同資產 50%，家庭檢視只計完整資產一次，不能計成兩倍。
6. **投入停止案例**：A 的投入採 `ownerRetirement`，候選退休月份起停止；B 的投入依自身結束規則繼續。
7. **月份順序案例**：月底投入在投入當月不產生報酬，下一月份才開始計息。
8. **不可達案例**：所有候選月份到規劃終點都出現資金缺口時，回傳 `notAchievableWithinHorizon`。
9. **可重現案例**：相同輸入、契約版本與規則版本必須產生相同 `inputHash` 與相同結果。

---

## 九、交付判定

Codex（程式開發代理）完成第一版計算核心時，至少需交付：

- 獨立於 UI（使用者介面）與 IndexedDB（瀏覽器本機資料庫）的純計算模組。
- 本附錄八項以上的單元測試。
- 可檢查每月數值的時間軸輸出。
- 契約版本、規則版本與輸入摘要。
- 錯誤、提醒、納入資料與排除資料清單。

達成以上條件，才可把計算結果接到儀表板並作為第一版退休年齡顯示依據。
